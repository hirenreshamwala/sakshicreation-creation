import { ExpandedRowFormProps} from "@/constants/interface";
import { Box } from "@mui/material";

export const ExpandedRowForm = ({ row, setEditData, setOpen }: ExpandedRowFormProps) => {

    return (
        <Box sx={{ p: 2, backgroundColor: "#f9fafb" }}>
         sell order row
        </Box>
    );
};